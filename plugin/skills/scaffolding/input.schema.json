{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Scaffolding Input",
  "description": "Input parameters for scaffolding project components based on discovered component configurations.",
  "type": "object",
  "properties": {
    "project_name": {
      "type": "string",
      "description": "Name of the project being scaffolded"
    },
    "target_dir": {
      "type": "string",
      "description": "Absolute path to the target directory"
    },
    "components": {
      "type": "array",
      "description": "Components to scaffold with their type and name",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "description": "Component type (e.g., server, webapp)"
          },
          "name": {
            "type": "string",
            "description": "Component name identifier"
          }
        }
      }
    },
    "project_description": {
      "type": "string",
      "description": "Brief description of the project"
    },
    "primary_domain": {
      "type": "string",
      "description": "Primary business domain for the project"
    },
    "skills_dir": {
      "type": "string",
      "description": "Path to the plugin skills directory for template resolution"
    }
  },
  "required": [
    "project_name",
    "target_dir",
    "components"
  ]
}
