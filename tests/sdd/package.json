{
  "name": "sdd-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:fast": "vitest run fast/",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
